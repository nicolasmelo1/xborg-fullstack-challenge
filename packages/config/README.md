## @xborg/config

Shared configuration package for the monorepo.

### Tech

- TypeScript

### What it contains

- Shared TypeScript `tsconfig` presets
- Shared ESLint flat configs (used by `packages/web`, `packages/api-gateway`, `packages/auth-microsservice`, etc.)
